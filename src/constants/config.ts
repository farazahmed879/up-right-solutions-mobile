// Note: In a real project with react-native-dotenv or similar,
// you would import these from environment variables.
// For now, we'll use a constant.

export const Config = {
  API_BASE_URL: 'https://your-api-domain.com/api', // Replace with your actual base URL
  TOKEN_KEY: '@auth_token',
  REFRESH_TOKEN_KEY: '@refresh_token',
  USER_KEY: '@user_data',
};
